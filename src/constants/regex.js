export const EMAIL_PATTERN = /^[\w._%+-]+@[\w.-]+\.[a-zA-Z]{2,}$/;

export const PASSWORD_PATTERN = /^.{8,30}$/;

export const NUMBER_ONLY_PATTERN = /^\d+$/;

export const TEXT_ONLY_PATTERN = /^[a-zA-Z\s]+$/;

export const PHONE_PATTERN = /^\d{10,11}$/;